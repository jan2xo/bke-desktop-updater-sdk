# BKE Desktop Updater SDK

STATUS: SCAFFOLD / PRE-API

Foundation repository for the future `BKE.Desktop.UpdaterClient` package. No updater implementation, downloads, elevation, keys, trusted URLs, privileged execution, licensing, entitlement, or update authority is included.

Future transport will remain behind the BKE Licensing Agent and Updater Core authority chain. The public API is intentionally not frozen and will be derived from certified BKE products and Agent protocols after evidence review.

The intended package is `BKE.Desktop.UpdaterClient` version `0.1.0`, targeting .NET 8. No package feed or production publication is implied.